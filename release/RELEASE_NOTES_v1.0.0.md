# Release notes for Version 1.0.0

## Headline

For every fixed \(p\in(1/2,1)\), the random series-parallel expected-distance
growth factor is the attained maximal eigenvalue of an explicit homogeneous
sum/min operator on normalized quantile profiles. The same value is given by
matching lower and upper Collatz--Wielandt formulas and a compatible
invariant-measure variational formula.

Prior work supplies the critical endpoint \(\delta(1/2)=0\), and the
all-series endpoint \(\delta(1)=\log 2\) is elementary. The Version 1.0.0
contribution is the all-interior model-specific characterization, not a new
generic nonlinear Perron--Frobenius theorem.

## Frozen evidence for Version 1.0.0

- manuscript PDF and designated TeX source;
- canonical repaired proof and exact scope contract;
- public-safe mathematical and literature audits;
- repository and deterministic evidence-bundle checksum ledgers; and
- formalization artifacts only as feasibility/request material, with no
  kernel-checked theorem claim.

## Scope and status

The mathematical audit, three priority-search lanes, exact-source PDF build,
and page-by-page visual preflight are complete. The model-specific assembly is
described as apparently new after documented search through 2026-08-09, with
moderate confidence; absolute historical priority, human peer review, and
formal verification are not claimed. The repository is public; both workflows
passed at public-`main` commit
`d79230cdb87d1438c97281d0c940f8e1a352642c`, and exactly the verify and PDF
badges were anonymously tested. The later metadata/evidence closure must
re-pass CI after merge. The immutable Version 1.0.0 tag and GitHub release,
published-asset re-download verification, DOI deposit, and any external notice
remain pending and are not claimed by these preparatory notes.
